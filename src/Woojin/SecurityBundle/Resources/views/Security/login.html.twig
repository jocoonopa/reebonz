<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="utf-8" />
  <title>Reebonz系統登入頁面</title>
  <link rel="stylesheet" href="/bundles/woojinbackend/css/bootstrap.yeti.min.css">
  <script src="/js/reebonz.js"></script>
</head>
<body>

<div class="modal" style="display: block !important;">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
    
        <h4 class="modal-title">Reebonz</h4>
      </div>
      <div class="modal-body">
        {% if error %}
        <div class="alert alert-danger alert-dismissable">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <strong>帳號或密碼有錯誤喔 ! </strong>
        </div>
        {% endif %}

        <form action="{{ path('login_check') }}" role="form" method="post" >
          <div class="form-group">
            <label for="username">帳號</label>
            <input type="text" id="username" class="form-control" name="_username" value="{{ last_username }}" placeholder="請輸入帳號" />
          </div>
          <div class="form-group">
            <label for="password">密碼</label>
            <input type="password" id="password" name="_password" class="form-control" placeholder="請輸入密碼" />
            <input type="hidden" name="_target_path" value="/order" />
          </div>
        </form>
      </div>

      <div class="modal-footer">
        <a class="btn btn-default forgot" href="{{ path('forgotten') }}">忘記密碼</a>
        <button type="button" class="btn btn-primary submit">登入</button>
      </div>
    </div>
  </div>
</div>
<script>
$(function () {
  $('button.submit').click(function () {
    $('form').submit();
  });
});
</script>
</body>
</html>