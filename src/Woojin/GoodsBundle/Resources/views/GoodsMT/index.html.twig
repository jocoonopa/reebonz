{% verbatim %}
<div class="row">
	<div ng-show="successMsg || errorMsg;" class="msgFix">
		<div ng-if="successMsg;" class="alert alert-success no-margin" role="alert">{{ successMsg }}</div>
		<div ng-if="errorMsg;" class="alert alert-danger no-margin" role="alert">{{ errorMsg }}</div>
	</div>

	<form class="form-inline" role="form">
		<div class="form-group">
		  <input ng-model="query.name" type="text" name="filter" class="form-control input-sm" placeholder="材質"/>

		  <button ng-click="clean(); create(query);" class="btn btn-primary btn-sm">新增</button>
  	</div>
	</form>
	
	<p>&nbsp;</p>

	<table class="table table-hover">
		<tr>
			<td ng-click="orderProp='id'; reverse=!reverse;" class="pointer">id</td>
			<td ng-click="orderProp='name'; reverse=!reverse;" class="pointer">材質</td>
			<td>
				動作
			</td>
		</tr>

		<tr ng-repeat="goodsMT in goodsMTs | filter:query | orderBy:orderProp:reverse" class="listing">
			<td>{{ goodsMT.id }}</td>
			<td><input ng-focus="setTmp(goodsMT);" ng-blur="update(goodsMT);" ng-model="goodsMT.name" type="text" class="input-sm no-border"></td>
			<td>
				<button ng-click="clean(); destroy(goodsMT);" class="btn btn-danger btn-sm">刪除</button>
			</td>
		</tr>
	</table>
</div>
{% endverbatim %}