{% verbatim %}
<div class="form-group" ng-show="model[name]">
	<label class="col-sm-2 control-label">{{ label }}</label>

	<div class="col-sm-10">
		<span class="{{ spanClass }}">{{model[name]}}</span>
	</div>
</div>
{% endverbatim %}